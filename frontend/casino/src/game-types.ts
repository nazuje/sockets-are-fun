export type GameStart = {
  id: string;
  status: string;
};

export type GameEnd = {
  id: string;
  status: string;
};

export type NewGame = {
  id: string;
  status: string;
};

export type Message = {
  id: string;
  message: string;
  timestamp: number;
};
